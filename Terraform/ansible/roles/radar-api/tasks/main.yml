- name: Place Docker compose template
  template:
    src: docker-compose.yml.j2
    dest: /containers/radar-api/docker-compose.yml

- name: Start containers
  community.docker.docker_compose_v2:
    project_src: /containers/radar-api
    pull: always
