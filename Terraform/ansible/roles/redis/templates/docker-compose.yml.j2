services:
    redis:
        image: redis:8.0.0
        container_name: redis
        restart: always
        ports:
            - 6379:6379
        volumes:
            - ./data:/data
        networks:
            - {{ shared_docker_network }}
networks:
    {{ shared_docker_network }}:
        external: true
